[package]
name = "ferros_hal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Hardware abstraction layer for ferros — MMIO, UART, framebuffer, storage"

[dependencies]
ferros_vault = { path = "../ferros_vault", default-features = false }

[features]
default = ["std"]
std = ["ferros_vault/std"]
