[package]
name = "ferros_pt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Photon Transport — reliable chunked VSF transfer over USB bulk"

[dependencies]
blake3 = { version = "1", default-features = false, features = ["pure"] }
ferros_ledger = { path = "../ferros_ledger" }

[features]
default = []
std = []
