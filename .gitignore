target/
*.img
*.bin
*.signed

# Keys — never push secrets
keys/*.secret
keys/

# Editor / IDE
.vscode/
.idea/

# Patent / IP — build artifacts only (source + PDFs are public)
Intellectual Property/*.log
Intellectual Property/*.aux
Intellectual Property/*.out
Intellectual Property/*.synctex.gz

# Signed binaries
kernel.signed

# Temp / generated
*.tmp.*
compact.txt

# Local memory — not for repo
memory/
CLAUDE MEMORY.md
tools/pixel8/
texput.log
