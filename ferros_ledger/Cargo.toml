[package]
name = "ferros_ledger"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Append-only event chain — BLAKE3-chained VSF entries, capability-gated"

[dependencies]
blake3 = { version = "1", default-features = false, features = ["pure"] }

[features]
default = []
