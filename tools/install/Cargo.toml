[package]
name = "ferros-install"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Format and bootstrap a ferros partition from macOS"

[[bin]]
name = "ferros-install"
path = "src/main.rs"

[dependencies]
ferros_layout = { path = "../../ferros_layout" }
blake3 = "1"
