[package]
name = "ferros_miro"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "miro_bench"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

# Detached from the ferros workspace: this is a measurement/prototype crate.
# The real crate will fold into the workspace as no_std with the ferros BLAKE3.
[workspace]
