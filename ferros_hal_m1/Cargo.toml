[package]
name = "ferros_hal_m1"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ferros HAL — Apple M1 (m1n1 EL2, aarch64-unknown-none)"

[dependencies]
ferros_hal = { path = "../ferros_hal", default-features = false }
